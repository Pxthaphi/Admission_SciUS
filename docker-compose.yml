services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    image: scius-frontend:latest
    ports:
      - "3001:3000"
    env_file:
      - .env
    environment:
      - NODE_ENV=production
      - HOSTNAME=0.0.0.0
    restart: unless-stopped
